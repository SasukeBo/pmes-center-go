package ftpconn

import (
	// "fmt"
	// "io/ioutil"
	"testing"
)

func TestReadFile(t *testing.T) {
	// r := readFile("test-file.txt")
	// buf, err := ioutil.ReadAll(r)
	// if err != nil {
	// 	t.Errorf("ReadAll failed:\n%v", err)
	// }
	// fmt.Printf("%s\n", string(buf))

	// if string(buf) != "Hello World" {
	// 	t.Errorf("want Hello World, get: %v", string(buf))
	// }

	getList()
}
